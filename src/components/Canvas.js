import React from 'react';
import styled from 'styled-components';

const CanvasWrapper = styled.div`flex-grow: 1; background-color: #ffffff; position: relative;`

const Canvas = () => {
return <CanvasWrapper />;
};

export default Canvas;